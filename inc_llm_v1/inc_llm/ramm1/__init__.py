"""Universal Ramm1 — LLM + RAMM1 OS + Builder + Memory + Scraper + Hybrid Link API.

One integrated build containing six inseparable parts:
1. RAMM1 OS — userspace resource allocator (3.5 GB RAM reservation + Universal RAM Supply)
2. Universal Ramm1 LLM runtime — adaptive inference + OpenAI/MCP API for external big models
3. Autonomous Builder — non-stop ecosystem builder (never quits)
4. Universal Memory + Recursive Link — gets smarter every use via the mesh
5. Ramm1 Web Scraper — new, way better than current + browser extension
6. Hybrid API for Universal Recursive Link — talks to ANY LLM, viral propagation

All six live in one build, one process, one install.
"""

from __future__ import annotations

__version__ = "1.0.0"
__all__ = ["__version__"]
