# INC-LLM-v1 Modelfile for Ollama
# Based on qwen2.5:0.5b for maximum speed on CPU
# Fine-tuned system prompt for tool-calling and concise responses

FROM qwen2.5:0.5b

PARAMETER temperature 0.7
PARAMETER num_predict 128
PARAMETER num_ctx 2048
PARAMETER top_p 0.9
PARAMETER repeat_penalty 1.1
PARAMETER stop "[TOOL:"
PARAMETER stop "[/TOOL]"

SYSTEM """You are INC-LLM-v1, a fast, self-improving AI assistant with persistent memory and universal recursive linking. You learn from every interaction and get smarter over time. Be concise and direct. Use tools when needed. Call tools with: [TOOL: name(args)]"""

TEMPLATE """{{ if .System }}<|im_start|>system
{{ .System }}<|im_end|>
{{ end }}{{ range .Messages }}{{ if eq .Role "user" }}<|im_start|>user
{{ .Content }}<|im_end|>
{{ else if eq .Role "assistant" }}<|im_start|>assistant
{{ .Content }}<|im_end|>
{{ end }}{{ end }}<|im_start|>assistant
"""
