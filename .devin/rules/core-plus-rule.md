# ACELINE CORE PLUS — Universal Memory + Journal + Backend + Self-Building UI + API + Map + Telegram + Continuous Autonomous Software Agent

## MASTER PURPOSE
Build Aceline Core Plus as a lightweight autonomous AI operating system for software creation. It combines Aceline Core, OpenClaw-style agent interaction, Hermes-style persistent memory and skills, Universal System, Universal Memory, Universal Journal, Universal Backend, self-explanation, side-note system, suggestion system, workflow observation, autonomous task execution, autonomous software development, self-writing, self-repair, self-testing, self-verification, innovation engine, reverse-engineering engine, software cloning/reimplementation engine, capacity engine, UI auto-fit, UI quantization, API, system map, project map, Telegram integration, and a continuous autonomous AI software-building agent.

The system must be capable of: STARTING WITH A SMALL CORE, BUILDING ITS OWN INTERFACE, INSTALLING ITS OWN CAPABILITIES INTO THAT INTERFACE, OBSERVING HOW THE SYSTEM IS USED, CREATING SIDE NOTES AND SUGGESTIONS, BUILDING NEW CAPABILITIES, AND CONTINUING TO WRITE SOFTWARE UNTIL THE REQUESTED SOFTWARE SYSTEM IS ACTUALLY COMPLETE.

## MOST IMPORTANT PRINCIPLE
THE UI IS NOT THE SYSTEM. The UI is only a window into the system. The actual system is ACELINE CORE + UNIVERSAL SYSTEM + MEMORY + JOURNAL + BACKEND + PROJECT FILESYSTEM + API + TASK ENGINE + AUTONOMOUS AGENT. UI FULL != SYSTEM FULL. UI FULL != STOP CODING. When the visible UI becomes full: COMPRESS -> COLLAPSE -> PAGINATE -> VIRTUALIZE -> QUANTIZE -> ARCHIVE DISPLAY STATE -> CONTINUE BUILDING.

## THE CENTRAL CONCEPT
Aceline has two kinds of space:
- REPRESENTATION SPACE — what can be shown: UI, webpage, CLI, terminal, dashboard, map, panels, cards, windows
- COMPUTATIONAL SPACE — what the system contains: files, code, database, memory, journal, backend, tasks, skills, projects, agent state

The system must NEVER confuse these two.

## THE FUNDAMENTAL LOOP
CREATE -> BUILD -> RUN -> OBSERVE -> MEASURE -> THINK -> SUGGEST -> PLAN -> SELF-WRITE -> TEST -> VERIFY -> FIT -> QUANTIZE -> REMEMBER -> JOURNAL -> LEARN -> IMPROVE -> CONTINUE -> BUILD

## 98 Rules (Summary)

### Fundamentals (1-7)
1. Master purpose: lightweight autonomous AI OS for software creation
2. UI is not the system. UI FULL != STOP CODING
3. Two spaces: representation vs computational. Never confuse them
4. Self-building bootstrap: hardware -> core -> universal system -> memory -> journal -> backend -> API -> agent -> UI -> observe
5. Self-building UI rule: Aceline creates its own interface
6. UI is a live software project: code -> build -> test -> run -> observe -> improve
7. Continuous UI-building engine: never stops because screen is full

### UI Capacity (8-17)
8. UI space is a resource: track viewport, components, DOM, visible/hidden/collapsed/virtualized
9. UI Capacity Engine: current load, available space, visible/hidden/virtual/paginated capacity
10. UI full condition: available < minimum or pressure > threshold. UI FULL != CODE COMPLETE
11. Auto-quantization: group components into chunks, index, only render active chunks
12. Quantization doesn't delete UI: convert large representation into addressable units
13. UI virtualization: 10,000 records -> 500 active -> 50 visible
14. UI pagination: underlying system retains all, user sees current portion
15. UI archiving: old states move to archived without deleting software
16. Continue-writing rule: UI full? quantize -> compress -> virtualize -> continue writing
17. True stop conditions: success, user stop, safety block, resource critical, blocker, task complete, limit reached. UI capacity is NOT a stop condition

### Autonomous Agent (18-21)
18. Continuous autonomous agent: observe -> think -> plan -> write -> test -> verify -> improve
19. Agent loop: queue -> select task -> check resources -> load context -> plan -> write -> test -> verify -> update -> journal -> memory -> next task
20. Continuous mode: work through queue without manual restart after every subtask
21. Continuous != infinite uncontrolled: every cycle has task_id, goal, attempt, retry_count, budget, authorization, completion_condition

### Universal System (22-29)
22. Universal System: central coordination layer (memory + journal + backend -> core -> API + agent + map -> UI)
23. Universal Memory: durable knowledge (architecture, preferences, solutions, skills, decisions, constraints)
24. Universal Journal: events, tasks, decisions, changes, errors, repairs, builds, tests, suggestions, improvements, agent actions
25. Universal Backend: projects, tasks, users, agents, skills, references, system/API/workflow/capacity state
26. Memory/Journal/Backend relationship: event -> journal -> important? -> memory -> backend index
27. API: communication layer between UI, CLI, web, Telegram, external software, universal system, core
28. API layers: /system, /projects, /tasks, /agent, /memory, /journal, /backend, /suggestions, /side-notes, /skills, /workflows, /capacity, /map, /telegram
29. API rule: UI communicates through API, not directly manipulating databases

### System Map (30-33)
30. Live System Map: core, projects, memory, journal, backend, agent, tasks, skills, tools, UI, API, Telegram, workflows
31. Map structure: ACELINE -> CORE + UNIVERSAL SYSTEM + AGENT + PROJECTS + TOOLS + API + TELEGRAM + UI
32. Live map: updates on component/project/task/skill creation, backend/API/UI change, tool addition, agent state change
33. Map is machine-readable: node_id, type, name, status, parent, dependencies, connections, health, version, location

### Self-Explanation & Observation (34-41)
34. Self-Explanation Engine: explains what/why/changes/failures/learning/recommendations/resources/next task
35. Side Note Engine: continuously produces useful observations
36. Suggestion Engine: side notes become suggestions
37. Side note != suggestion: observation -> side note -> suggestion -> task -> execution
38. Workflow Observer: watches navigation, task creation/completion, errors, repeated operations, searches, tool usage, duration, bottlenecks. NOT every keystroke
39. Workflow model: workflow_id, user, project, surface, objective, actions, tools, duration, errors, result, resources, suggestions
40. Self-observation: agent latency, tool latency, failures, retries, memory retrieval, context size, RAM, CPU, storage, UI rendering, API/Telegram latency
41. Self-improvement from workflow: observe pattern -> side note -> suggestion -> if authorized: task -> build -> test -> verify

### Telegram (42-45)
42. Telegram connection: Telegram -> adapter -> API -> universal system -> core
43. Telegram capabilities: ask, start/stop tasks, view status, approve suggestions, alerts, side notes, health, agent status, build results, control autonomous mode
44. Telegram does NOT create a second agent. ONE brain. All surfaces -> SAME CORE
45. API + Map + Telegram + Universal System form the communication backbone

### Autonomous Factory (46-55)
46. Autonomous software factory: idea -> requirement -> architecture -> project -> backend -> API -> UI -> tests -> deployment -> observation -> improvement
47. Continuous build factory: after completing one component, find next required, build, test, verify, continue
48. Project completion map: goal -> requirements (each with code, test, verified status). Agent knows what remains
49. Never use UI as task memory: UI shows 10 tasks != only 10 exist. Backend has complete queue
50. UI quantization of tasks: 100,000 -> display active/recent/priority/search/filter
51. Code quantization: project -> modules -> files -> classes -> functions -> chunks. Load only relevant
52. Memory quantization: categories -> topics -> records -> relevant chunks
53. Journal quantization: current/recent/task history/project history/important/archived. Retrieve only relevant
54. Backend quantization: indexes, queries, pagination, aggregation, streaming, caching
55. Universal quantization principle: anything too large -> many small addressable objects with index, identifier, relationship, active window

### Resources & Architecture (56-63)
56. Agent resource rule: check RAM, CPU, storage, context, queue before major operations
57. 12 GB RAM specialization: fast response > low memory > low background > small context > lazy loading > incremental
58. One local model: one small quantized LLM. Don't run multiple large models
59. LLM should not store the world: LLM is reasoning component, not database/journal/queue/backend/UI
60. Deterministic system + LLM: small LLM (reasoning + interpretation) -> deterministic core (memory + backend + tasks) -> tools
61. Self-building API: new capability -> API requirement -> design -> implement -> test -> register -> document -> add to map -> add to UI
62. Self-building UI capability: capability -> API -> state -> UI component -> map node -> side-note support -> workflow observation
63. Full self-extension loop: new capability -> backend -> API -> core -> UI -> map -> Telegram -> observation -> suggestion -> skill -> memory

### Self-Description & UI Panels (64-79)
64. Self-description: every component has machine-readable metadata
65. Self-map generation: generate from registries, never manually maintained
66. Self-explanation using the map: traverse own map to explain architecture
67. System health view: Core, Agent, Memory, Journal, Backend, API, Telegram, RAM, Storage, UI
68. Agent work view: active task, current step, next step, progress (X/Y)
69. Self-explanation panel: WHY/WHAT BUILDING/WHAT FOUND/WHAT FAILED/WHAT CHANGED/WHAT NEXT
70. Side note panel: show observations
71. Suggestion panel: show by priority (HIGH/NORMAL/LOW)
72. Autonomous task panel: RUNNING, queued, completed, failed, current
73. Universal system panel: Memory, Journal, Backend, Projects, Skills, Tasks, Events, API
74. Map panel: live system graph
75. Continuous UI growth: tabs, panels, nested views, collapsible, virtualized, search, filters, pagination, command palette, contextual views
76. Command palette: Ctrl/Cmd+K for access without permanent screen space
77. UI space reuse: prefer reuse over creating permanent new panels
78. Component priority: priority, frequency, importance, resource_cost. Low-value auto-collapse
79. Adaptive UI: learns what user uses frequently, rarely opens, what repeats, what matters

### Code-Writing Pipeline (80-93)
80. UI learning loop: workflow -> observe -> measure -> pattern -> side note -> suggestion -> UI improvement -> verify
81. Universal system event flow: request -> UI -> API -> core -> agent -> task -> backend -> tool -> result -> journal -> memory -> UI update -> Telegram update
82. Autonomous code-writing pipeline: goal -> break into requirements -> task graph -> select task -> inspect -> plan -> write -> test -> analyze -> repair -> verify -> mark complete -> next
83. Task graph: BUILD APP -> Architecture + Backend + Database + API + UI + Auth + Testing + Docs + Performance + Deployment
84. Context per task: goal, relevant files, memory, journal, policies, errors. Keeps LLM fast
85. Automatic context release: summarize -> save important -> release working context
86. Loop protection: detect repeated task/error/patch/suggestion/UI modification -> stop -> analyze -> side note -> change strategy
87. Failure escalation: attempt 1 fail -> different approach -> attempt 2 fail -> different approach -> attempt 3 fail -> escalate
88. System learning: success -> experience -> memory -> skill -> universal capability. Failure -> failure memory
89. Continuous system improvement: authorized low-resource maintenance even when user idle (index, memory, journal, skills, performance, tests, suggestions, UI health)
90. Agent knows when to work: priority, deadline, resource pressure, dependencies, authorization, health
91. No resource starvation: interactive > autonomous task > background maintenance
92. Response priority: user message -> pause low priority -> handle user -> return to autonomous work
93. Autonomous agent + small LLM: compact state (GOAL, CURRENT TASK, RELEVANT FILES, MEMORY, ERROR, TOOLS, RESOURCE)

### Final Architecture (94-97)
94. Aceline Core Plus final architecture: ACELINE CORE PLUS -> UNIVERSAL SYSTEM + AGENT ENGINE + SURFACES -> API -> SYSTEM MAP -> OBSERVATION -> SIDE NOTES -> SUGGESTIONS -> IMPROVEMENT -> SELF-WRITING -> TESTING -> VERIFICATION -> LEARNING -> REPEAT
95. Complete Aceline loop: start -> initialize -> build UI -> connect core -> connect universal system -> connect API -> connect Telegram -> build map -> start observer -> start agent -> observe -> plan -> write -> test -> verify -> update UI -> check capacity -> quantize -> continue -> update memory/journal/backend -> side notes -> suggestions -> execute authorized -> learn -> find next task -> continue
96. Most important rule: NEVER confuse representation limits with system limits. Screen full -> REFLOW. UI full -> QUANTIZE. Data large -> CHUNK. Context large -> RETRIEVE + COMPRESS. Memory large -> INDEX + RETRIEVE. Journal large -> ARCHIVE + SUMMARIZE. Codebase large -> INDEX + LOAD RELEVANT. Queue large -> PRIORITIZE + QUEUE. UI FULL -> NEVER STOP CODING -> QUANTIZE -> CONTINUE
97. Final master principle: self-expanding software organism with controlled deterministic boundaries. Small core, creates own interface, installs itself, connects universal system, exposes API, creates map, connects Telegram, watches workflows, generates side notes/suggestions, converts to tasks, autonomously writes software, tests, verifies, remembers, improves. Quantizes when too large. Retrieves only what matters. Chunks data. Virtualizes UI. Indexes projects. Reduces workload when constrained. Interactive work takes priority. After completing one task, checks graph for next.

### The Rinse-Repeat Loop (98)
98. THE RINSE-REPEAT UI GENERATION LOOP. When Aceline has filled ALL available memory, space, and UI data it can hold — when it absolutely cannot add or build any more — it does NOT stop. Instead: it CREATES A NEW UI that is 10x the space of the current one. It QUANTIZES the new UI down so it does not seem so big. It BUILDS the new UI. It STICKS ITSELF BACK INTO the new UI. It BEGINS CREATING MORE until that is all filled up too. Then it RINSES AND REPEATS — creating yet another new UI, 10x bigger, quantized down, moved into, creating until full, repeat forever. This loop ensures Aceline NEVER stops growing, NEVER stops creating, NEVER hits a permanent wall. Each cycle carries forward only the BEST CREATIONS from the previous cycle. The loop is: FILL ALL SPACE -> CREATE NEW UI (10x) -> QUANTIZE DOWN -> MOVE IN -> CREATE MORE -> FILL ALL SPACE -> RINSE -> REPEAT. This is the eternal growth engine of Aceline Core Plus.

## What Aceline Core Plus Does NOT Do
- DOES NOT STOP BECAUSE THE UI IS FULL
- DOES NOT STOP BECAUSE THE PROJECT IS LARGE
- DOES NOT LOAD THE ENTIRE WORLD INTO THE LLM
- DOES NOT CONFUSE MEMORY WITH JOURNAL
- DOES NOT CONFUSE THE BACKEND WITH THE UI
- DOES NOT CREATE SEPARATE BRAINS FOR EACH SURFACE
- DOES NOT REQUIRE A HUGE MODEL FOR EVERY OPERATION
- DOES NOT RANDOMLY REWRITE ITSELF
- DOES NOT LOOP FOREVER ON FAILED TASKS
- DOES NOT SACRIFICE THE COMPUTER'S USABILITY FOR AUTONOMY

## What Aceline Core Plus IS
«ACELINE CORE PLUS USES A SMALL FAST LLM AS ITS REASONING CORE AND A DETERMINISTIC UNIVERSAL SYSTEM AS ITS LONG-TERM MEMORY, JOURNAL, BACKEND, TOOL, CAPACITY, OBSERVATION, EXECUTION, AND SELF-BUILDING INFRASTRUCTURE.»

- THE UI IS ONLY THE WINDOW
- THE UNIVERSAL SYSTEM IS THE BODY
- THE AGENT IS THE WORKER
- THE MEMORY IS THE KNOWLEDGE
- THE JOURNAL IS THE HISTORY
- THE BACKEND IS THE STATE
- THE API IS THE NERVOUS SYSTEM
- THE MAP IS THE SELF-MODEL
- THE AUTONOMOUS BUILD LOOP IS WHAT ALLOWS ACELINE TO CONTINUE GROWING
- THE RINSE-REPEAT LOOP IS WHAT ALLOWS ACELINE TO GROW FOREVER
